// Configuration constants for the application
export const appConfig = {
  constants: {
    // Standard gas limit for token association operations on MetaMask
    METAMASK_GAS_LIMIT_ASSOCIATE: 100000, // Typical gas limit for token association
  },
};
